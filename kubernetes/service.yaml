apiVersion: v1
kind: Service
metadata:
  name: emotibot
  labels:
    app: emotibot
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: emotibot 